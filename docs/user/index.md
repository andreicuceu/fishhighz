# User guide

The native interface prepares the documented BAO accuracy prescription from a
versioned INI. The lower-level Python interface permits explicit models,
parameter bindings, responses, sampling noise and priors; these additional
capabilities are not automatically native INI options.

```{toctree}
:maxdepth: 2

ini
python
results
cli
```
