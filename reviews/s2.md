# S2 — Weight convergence and numerical refinement

**All six revised-accuracy bins pass every prescribed isolated and combined
finite-refinement criterion.** All required forest preparations converge: QSO
forests in bins 1–6 and LBG forests in bins 2–6. There are no capped, arithmetic,
ineligible or unresolved trials. The largest individual BAO-error refinement
change is **0.0011411%**; the largest joint change is **0.0012685%**, both in
the combined refinement in bin 4. These are comfortably below the 0.1% target.
The maximum relative joint Fisher change is 2.4530e-5; maximum volume change is
2.0992e-11, below 1e-6. These statements concern finite numerical refinement
within the retained physical/input model, not continuum bounds or scientific
acceptance of that model.

The missing selected-bin-1 fixed-compatibility magnitude diagnostic also passes:
213→425 nodes changes an individual error by at most **0.0041283%** and the
three-spectrum joint error by at most **0.0015122%**. The bins 2–6 historical
rectangular-grid evidence is reusable: all saved numerical operands retain
their hashes and S1 recovered the primary 107-node results bitwise.

## Numerical results

Bin labels below are 1–6; saved indices are 0–5. The bin-1 covariance contains
only lya(qso) auto, QSO auto and their cross; all 15 spectra enter bins 2–6.
Every individual result, including weak spectra, enters the refinement checks.

| Bin | k | mu | Magnitude | Volume | Derivative step | Weight rtol | Combined | Largest individual change (%) | Largest joint change (%) |
| --- | --- | --- | --- | --- | --- | --- | --- | ---: | ---: |
| 1 | pass | pass | pass | pass | pass | pass | pass | 0.000838984 | 0.000856832 |
| 2 | pass | pass | pass | pass | pass | pass | pass | 0.000820866 | 0.000999617 |
| 3 | pass | pass | pass | pass | pass | pass | pass | 0.000859367 | 0.001109280 |
| 4 | pass | pass | pass | pass | pass | pass | pass | 0.001141082 | 0.001268487 |
| 5 | pass | pass | pass | pass | pass | pass | pass | 0.001123117 | 0.001233695 |
| 6 | pass | pass | pass | pass | pass | pass | pass | 0.000868037 | 0.001016724 |

Every row's maximum comes from combined refinement. The largest individual
is the bin-4 LBG galaxy auto, transverse component, with signed change
−0.001141082%. The per-spectrum/per-component signed changes for every family
are retained in [refinement-summary.json](../.validation/s2/refinement-summary.json).
The table uses absolute changes; no plotting cut enters these checks.

All bins select the same final controls: 128 k intervals with order 4, mu order
32, volume order 32, composite magnitude order 16, finite-difference step
2.5e-4, and adaptive rtol=1e-5. Actual levels are k=32/64/128,
mu=8/16/32, magnitude=4/8/16, volume=8/16/32,
step=1e-3/5e-4/2.5e-4, rtol=1e-4/1e-5. The combined lower state uses
64/16/8/16/5e-4/1e-4 in the corresponding order. The initial upper levels
already pass, so no extension to the larger predeclared caps was needed.
Each bin has **13 actual successful trials**, all retained and bound to the
reported convergence operands by trial contract 3.

Final magnitude node counts are 2592, 2592, 2592, 2624, 2688 and 2640. Composite
orders describe nodes per integration partition, not total magnitude nodes.
The 8→16 magnitude change in BAO errors is at roundoff (at most 2.89e-15
fractionally); this is a result for the declared piecewise interpolation and
partition, not a transfer of rectangular-grid convergence to another measure.

### Actual adaptive stopping

Counts include updates after the seed. Each returned state includes the actual
doubled-candidate confirmation; no renormalization or absolute residual floor
was used. Counts are unchanged across the actual tested magnitude orders for
a given tolerance.

| Bin | QSO forest, rtol=1e-4 | QSO forest, rtol=1e-5 | LBG forest, rtol=1e-4 | LBG forest, rtol=1e-5 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 18 | 22 | excluded | excluded |
| 2 | 16 | 20 | 28 | 36 |
| 3 | 16 | 18 | 30 | 36 |
| 4 | 14 | 16 | 26 | 30 |
| 5 | 12 | 14 | 24 | 28 |
| 6 | 10 | 12 | 22 | 26 |

The largest BAO-error effect of tightening rtol is 2.7699e-8 percent for an
individual spectrum and 2.1637e-9 percent jointly, both in bin 2. The final
returned-state preceding-step residual is at most 1.068e-11 across all five
monitored quantities; the largest candidate-to-returned confirmation residual
is 1.586e-6, below 1e-5. Reports retain actual weights, coefficients, auxiliary
P/B/k/mu, controls, counts and residuals for every successful trial. QSO and LBG
forests use their own revised-accuracy inputs; no compatibility weights were
interpolated onto these grids.

## Fixed-compatibility magnitude diagnostic

The new [bin1-grid-bao.json](../.validation/s2/bin1-grid-bao.json) contracts only
the three retained spectra, using saved confirmed QSO-forest states on
107/213/425 nodes. It never prepares or forecasts the excluded LBG/LAE fields.
Its 12 forecasts comprise three selected individual spectra and one joint
result on each grid. Scalar Wick covariance agrees exactly in every cell;
independent direct joint solves agree within the declared 5e-12 bound.
Galaxy-only Fisher matrices are bitwise unchanged on every grid.

| Bin 1 refinement | Largest individual change (%) | Largest joint change (%) |
| --- | ---: | ---: |
| 107→213 | 0.002395929 | 0.000877410 |
| 213→425 | 0.004128334 | 0.001512161 |
| 107→425 | 0.001732307 | 0.000634737 |

The individual extrema are QSO-forest transverse errors. The final increment
changes sign and exceeds the preceding increment; all are small, but these
three grids do not establish monotonic convergence or a continuum bound.

For bins 2–6, the unchanged historical operand hashes and report hashes are
listed in `reused_bins` in the same file. Their accepted final 213→425 maximum
individual/joint changes remain 0.0843233%/0.00562013%, with the original
107-node primary retained. The old files were neither rewritten nor relabeled.

## Execution and reproducibility

One serial real run produced all **18** full-compatibility, fixed-compatibility
and accuracy bin records, allowing S3 to reuse the outputs. No forecast was
rerun. The exact reviewed S1 wheel was used throughout:
`.validation/s1/dist/fishhighz-0.1.0.dev0-py3-none-any.whl`, SHA256
`c19a81d867ffda8cef93f19808ac885e1aecbbdf20d713ef7cdd2cf164c9a8ae`.
No package source or wheel changed in S2; additions are the two focused
saved-input/summary scripts and this report.

```bash
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
FISHHIGHZ_FISHER_BACKEND=numba PYTHONPATH=../lyaforecast /usr/bin/time -v \
.validation/s1-s3-env/bin/python scripts/desi2_profiles.py run \
  --suite full --cases lya_qso_lbg_lae_15x2pt --bins 0 1 2 3 4 5 \
  --profiles full-compatibility fixed-compatibility accuracy \
  --accuracy-method early_lyaforecast --reference ../lyaforecast \
  --template ../vega/vega/models/Planck18/Planck18_z_2.406.fits \
  --reference-bundle .validation/step12-r2-implementation/reference-01 \
  --compatibility-bundle .validation/step12-r5-20260914T191855Z/profiles-checked \
  --wheel .validation/s1/dist/fishhighz-0.1.0.dev0-py3-none-any.whl \
  --output .validation/s2/profiles-r1
```

The user explicitly authorized login-node execution. All numerical libraries
were restricted to one thread. Actual wall time was **3m51.37s**, with 218.90s
user CPU and 6.41s system CPU, peak RSS **693440 kB**, and no swapping. Shared
CAMB preparation plus the first accuracy study took 125.17s; later accuracy
studies took 14.01–14.35s before evidence validation/writes. See
[profiles-r1.log](../.validation/s2/profiles-r1.log). The completed evidence
manifest is [profiles-r1/manifest.json](../.validation/s2/profiles-r1/manifest.json).

Additional checks, each single-thread:

- `python scripts/check_selected_bin1_grid_bao.py --output
  .validation/s2/bin1-grid-bao.json`: passed in 2.16s; no new model forecast.
- `python scripts/summarize_s2_refinement.py --bundle .validation/s2/profiles-r1
  --output .validation/s2/refinement-summary.json`: completed; retains every
  individual/component refinement rather than only maxima.
- Installed-wheel `python scripts/desi2_profiles.py check --output
  .validation/s2/profiles-r1`: **complete=true**, independently reconstructing
  saved covariance/Fisher and replaying actual trial schedules. See
  [offline-check.log](../.validation/s2/offline-check.log).
- Ruff check/format passed for the two new scripts. No Slurm, commits, pushes,
  old weighting investigations, other real reference cases, sensitivity suite
  or S4 attribution were run.

## S3 continuation

After S2 scientific review, **reuse this complete bundle without another real
forecast**. Run the reviewed `scripts/plot_three_profiles.py --help` and supply
`.validation/s2/profiles-r1` as its bundle, saving products under a new S3
output directory. It contains the selected individual and full selected-joint
Fishers, both BAO errors, correlations/rank, numerical status and weighting
records for all three profiles. S3 should produce the paired-cut comparison
plots, full tables and fixed/full, accuracy/full and accuracy/fixed differences;
interpret material differences without executing S4 attribution.

Stop for independent S2 scientific review.
